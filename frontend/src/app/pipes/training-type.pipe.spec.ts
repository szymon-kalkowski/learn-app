import { TrainingTypePipe } from './training-type.pipe';

describe('TrainingTypePipe', () => {
  it('create an instance', () => {
    const pipe = new TrainingTypePipe();
    expect(pipe).toBeTruthy();
  });
});
